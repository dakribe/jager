
interface JobApplicationCardProps {
  companyName: string;
  jobTitle: string;
  status: string;
  notes: string | null;
}

export default function JobApplicationCard({
  companyName,
  jobTitle,
  notes,
  status,
}: JobApplicationCardProps) {
  return (
    <div>
    </div>
  );
}
